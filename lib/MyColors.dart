import 'package:flutter/material.dart';

class MyColors {
  static const mainText = Colors.black;
  static const blueText = Color.fromRGBO(0, 122, 255, 1);
  static const pageCounterText = Colors.white;
  static const pageCounter = Color.fromRGBO(0, 0, 0, 0.6);
  static const priceBox = Color.fromRGBO(249, 224, 0, 1);
  static const subtitle = Color.fromRGBO(60, 60, 67, 0.6);
  static const divider = Color.fromRGBO(198, 198, 200, 1);
}